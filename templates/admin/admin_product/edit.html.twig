{% extends 'admin/base.html.twig' %}


{% block title %}Edition du produit
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row">
			<div class="col-12">
				<h1 class="mt-3">Edition du produit</h1>

				<a class="btn btn-primary my-3" href="{{ path('admin_product_index') }}">
					<span class="btn-label">
						<i class="fa fa-chevron-left"></i>
					</span>Retour à la liste</a>

				{{ include('admin/admin_product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

				{{ include('admin/admin_product/_delete_form.html.twig') }}

			</div>
		</div>
	</div>

{% endblock %}
