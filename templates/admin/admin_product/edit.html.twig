{% extends 'admin/base.html.twig' %}


{% block title %}Edition d'un produit
{% endblock %}

{% block body %}
	<div class="container py-5">
		<div class="row">
			<div class="col-12">
				<h1 class="h130px">Edition du produit</h1>

				<a class="btn btn-dark my-3" href="{{ path('admin_product_index') }}">
					<span class="btn-label">
						<i class="fa fa-chevron-left"></i>
					</span>Retour à la liste</a>

				{{ include('admin/admin_product/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

				{{ include('admin/admin_product/_delete_form.html.twig') }}

			</div>
		</div>
	</div>

{% endblock %}
