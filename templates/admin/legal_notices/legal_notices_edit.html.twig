{% extends 'admin/base.html.twig' %}

{% block title %}legal notice edit
{% endblock %}

{% block body %}
	<div class="container py-5">

		<div class="row">
			{% include "_messages_flash.html.twig" %}
			<div class="col-12 ">

				<a class="btn btn-dark" href="{{ path('admin_legal_notices') }}">Revenir à la liste</a>

			</div>

			<div class="col-sm-12 mt-3">
				<div class="card">
					<div class="card-header">
						<h5 class="card-title">Edition de document</h5>
					</div>
					<div class="card-body">
						{{ form_start(form) }}
						{{ form_widget(form) }}
						<button class="btn btn-primary mt-3" type="submit">Enregistrer</button>
						{{ form_end(form) }}
					</div>

					{#<div class="card-body">
					{{ form_start(form) }}
					{{ form_row(form.title) }} <!--<ou {{ form_widget(form.title) }}  >-->
					<button class="btn btn-primary mt-3" type="submit">Enregistrer</button>																				
					{{ form_end(form, {render_rest: false}) }}
					</div>#}

				</div>
			</div>

		</div>
	</div>

{% endblock %}
